# live server
on the project file on FS
- run cmd : 
            live-server public      (or the starting project file)

## babel
 on the project file on FS
 -run cmd : 
              babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch